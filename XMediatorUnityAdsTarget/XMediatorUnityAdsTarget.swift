// Intentionally empty.
// This wrapper target exists so the XMediatorUnityAds product can combine the
// XMediatorUnityAds binary target and carry their required
// system-framework link settings (binary targets cannot declare either).
